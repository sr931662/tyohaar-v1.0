/// Album name every in-app "save to gallery" download uses (via package:gal),
/// so downloaded content lands in its own "Tyohaar" album on the device
/// instead of dumping into the default Pictures/Camera Roll.
const String kTyohaarGalleryAlbum = 'Tyohaar';
